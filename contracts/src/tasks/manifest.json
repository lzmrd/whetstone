{
  "_comment": "One task. The harness reads this rather than guessing filenames, so what was measured is stated rather than inferred.",
  "id": "log256-bytelen/v1",
  "task": {
    "path": "contracts/src/tasks/Task.sol",
    "contract": "Candidate",
    "sig": "f(uint256)"
  },
  "baseline": {
    "path": "contracts/src/tasks/Baseline.sol",
    "contract": "Baseline"
  },
  "original": {
    "path": "contracts/src/tasks/Original.sol",
    "contract": "Original"
  },
  "mutation": {
    "description": "result is the NUMBER OF BYTES needed to represent x rather than the index of its highest non-zero byte, and zero reverts with ZeroHasNoBytes() instead of returning a value",
    "author": "Claude (Claude Code)",
    "_author_note": "\u26a0\ufe0f Model-written. R2 asks that authorship be a declared fact, not an assumption. Disclosed in AI_USAGE.md.",
    "applied_to": [
      "task",
      "baseline"
    ],
    "proof_1_baseline_equals_task": "must PASS",
    "proof_2_task_differs_from_original": "must REFUTE (kind=semantic)"
  },
  "kind": "semantic",
  "trivial": {
    "path": "contracts/src/tasks/Trivial.sol",
    "contract": "Trivial",
    "description": "the task with the dead overflow check removed -- one word, no understanding required",
    "proof_3_trivial_equals_task": "must PASS (if it fails the check is not dead and this reference is invalid)"
  }
}
