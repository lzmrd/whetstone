# Foundry's `forge` is shadowed by /usr/bin/forge (an unrelated ZOE tool).
# Source this before any forge command:  source .envrc.sh
export PATH="$HOME/.foundry/bin:$PATH"
export PATH="$PWD/.tools:$PATH"
