# secrets — never commit
.env
.env.*
!.env.example
*.key

# foundry
out/
cache/
cache_forge/
broadcast/
lib/

# node
node_modules/
dist/
*.tsbuildinfo

# subgraph
subgraph/build/
subgraph/generated/

# model patches and compiled runtime bytecode, per run
.run/

# harness local state (source of truth for retries, but not committed)
harness/.runs/

# os
.DS_Store

# spike tooling (large binaries, reproducible from .envrc.sh instructions)
.tools/
.venv-tools/
